use crate::domain::model::game::game_id::GameId;

pub struct Game {
    pub id: GameId,
    // teams: Vec<Team>,
    // raid: GameRaid,
    // score: GameScore,
    // started_at: DateTime<Utc>,
    // finished_at: Option<DateTime<Utc>>,
}
